# SQL Scripts

Use these scripts to provision the configuration database schema used by the API models and EF Core mappings.

## Script order

1. `001_create_dataset_config_tables.sql`

## Usage

Run against the `DatasetBuilderConfig` SQL Server database before starting the API when migrations are not being used.
